@PACKAGE_INIT@

get_filename_component(CODES_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" DIRECTORY)

#set(CODES_VERSION_MAJOR "@CODES_VERSION_MAJOR@")
#set(CODES_VERSION_MINOR "@CODES_VERSION_MINOR@")
#set(CODES_VERSION_PATCH "@CODES_VERSION_PATCH@")

# This is true when the package is still in the build directory (not installed)
if(CMAKE_CURRENT_LIST_DIR STREQUAL "@CODES_BINARY_DIR@")
  set(CODES_PACKAGE_IN_BUILD TRUE)
endif()

cmake_print_variables(CMAKE_CURRENT_LIST_DIR)
if(CODES_PACKAGE_IN_BUILD)
  include ( "${CMAKE_CURRENT_LIST_DIR}/src/model-net/CODES-targets.cmake" )
else()
  include ( "${CMAKE_CURRENT_LIST_DIR}/CODES-targets.cmake" )
endif()
